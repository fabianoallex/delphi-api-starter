# delphi-api-starter — Guia para Agentes de IA

@infra/CLAUDE.md

@CLAUDE.md

O primeiro import traz os padrões da infra (DTOs, Optionals, JsonMapper, Repository, Service,
Controller); o segundo traz o que é específico deste projeto (estrutura de pastas, migrations,
`.env`, mensageria). Mantido como referência dupla — sem depender de o agente resolver imports
aninhados dentro de `CLAUDE.md` — para garantir que o conteúdo da infra seja carregado mesmo que
o agente não siga referências dentro de referências.
